{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "The local loading screen has no filesystem or shell access.",
  "windows": ["main", "quick-note"],
  "permissions": ["core:default"]
}
